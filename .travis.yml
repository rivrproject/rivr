language: python
python:
- "3.6"
install:
- pip install pygments
- pip install nose
- pip install nose-cov
script: nosetests -sv tests --with-coverage --cover-package=rivr
after_success:
- pip install coveralls
- coveralls
