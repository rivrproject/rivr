[tool.poetry]
name = "rivr"
version = "0.10.0"
description = "microweb framework"
authors = ["Kyle Fuller <kyle@fuller.li>"]
license = "BSD"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
